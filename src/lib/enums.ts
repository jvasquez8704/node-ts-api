export enum DBProvider {
    FIREBASE = 'firebase',
    FIRESTORE = 'firestore'
}

export enum HttpStatusCode {
    BadGateway = 502,
    ServerError = 500,
    BadRequest = 400,
    Forbidden = 403,
    NotFound = 404,
    Created = 201,
    NoContent = 204,
    Success = 200
}